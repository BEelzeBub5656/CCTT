# CCTT 离线同步功能集成说明

## 功能概述

已创建离线数据同步功能，无需服务器即可在多台设备间同步数据。

### 实现方式
- 数据导出为JSON文件
- 通过微信/邮件/蓝牙等分享到其他设备
- 在其他设备上导入文件完成同步

### 核心文件
- `lib/services/offline_sync_service.dart` - 离线同步服务
- `lib/pages/offline_sync_page.dart` - 离线同步UI页面

## 集成步骤

### 1. 安装依赖

已在 `pubspec.yaml` 中添加了所需依赖：

```yaml
share_plus: ^10.1.3     # 文件分享
file_picker: ^8.1.6     # 文件选择器
```

运行安装命令：
```bash
flutter pub get
```

### 2. 在主页面添加入口

在 `lib/pages/home_page.dart` 中添加离线同步入口。有两种方式：

#### 方式A：在AppBar添加菜单项（推荐）

在主页面的 AppBar 中添加菜单按钮：

```dart
import 'offline_sync_page.dart';  // 添加导入

// 在 AppBar 的 actions 中添加
appBar: AppBar(
  title: const Text('库存管理'),
  actions: [
    // 现有的同步按钮
    IconButton(
      icon: const Icon(Icons.sync),
      onPressed: _sync,
    ),
    // 新增：离线同步菜单
    PopupMenuButton<String>(
      icon: const Icon(Icons.more_vert),
      onSelected: (value) {
        if (value == 'offline_sync') {
          Navigator.push(
            context,
            MaterialPageRoute(builder: (_) => const OfflineSyncPage()),
          );
        } else if (value == 'void_list') {
          _showVoidedOrders();
        }
      },
      itemBuilder: (context) => [
        const PopupMenuItem(
          value: 'offline_sync',
          child: Row(
            children: [
              Icon(Icons.import_export, size: 20),
              SizedBox(width: 8),
              Text('离线同步'),
            ],
          ),
        ),
        const PopupMenuItem(
          value: 'void_list',
          child: Row(
            children: [
              Icon(Icons.delete_outline, size: 20),
              SizedBox(width: 8),
              Text('已作废单据'),
            ],
          ),
        ),
      ],
    ),
  ],
),
```

#### 方式B：添加独立按钮

在主页面合适位置（如底部按钮区域）添加：

```dart
ElevatedButton.icon(
  onPressed: () {
    Navigator.push(
      context,
      MaterialPageRoute(builder: (_) => const OfflineSyncPage()),
    );
  },
  icon: const Icon(Icons.import_export),
  label: const Text('离线同步'),
  style: ElevatedButton.styleFrom(
    backgroundColor: Colors.blue,
    foregroundColor: Colors.white,
  ),
),
```

### 3. 权限配置

#### Android (`android/app/src/main/AndroidManifest.xml`)

```xml
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 文件访问权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    
    <application>
        <!-- 文件提供者配置 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
```

创建 `android/app/src/main/res/xml/file_paths.xml`：

```xml
<?xml version="1.0" encoding="utf-8"?>
<paths>
    <external-path name="external_files" path="."/>
    <files-path name="internal_files" path="."/>
</paths>
```

#### iOS (`ios/Runner/Info.plist`)

```xml
<key>NSPhotoLibraryUsageDescription</key>
<string>需要访问相册以保存备份文件</string>
<key>UIFileSharingEnabled</key>
<true/>
<key>LSSupportsOpeningDocumentsInPlace</key>
<true/>
```

## 使用流程

### 场景1：从手机A同步到手机B

1. **在手机A上：**
   - 打开应用 → 点击"离线同步"
   - 点击"导出数据并分享"
   - 选择微信/邮件等方式发送文件

2. **在手机B上：**
   - 接收并保存文件到手机
   - 打开应用 → 点击"离线同步"
   - 点击"从文件导入数据"
   - 选择接收到的JSON文件
   - 选择"合并"策略（推荐）
   - 查看导入结果统计

### 场景2：定期备份

1. 定期导出数据
2. 文件自动保存在应用目录
3. 可在"本地备份文件"列表中查看
4. 需要恢复时，直接从列表导入

## 冲突解决策略

### Merge（合并）- 推荐
- 对比时间戳，使用最新的数据
- 适用于多设备协同工作
- 智能避免数据丢失

### Skip（跳过）
- 保留本地数据，跳过导入文件中的重复项
- 适用于只想补充缺失数据的场景

### Replace（替换）
- 用导入文件的数据覆盖本地
- 适用于恢复备份或从主设备同步到从设备

## 数据格式

导出的JSON文件结构：

```json
{
  "version": "v2.0",
  "exportTime": 1724567890000,
  "warehouses": [
    {
      "id": "uuid",
      "name": "仓库名称"
    }
  ],
  "orders": [
    {
      "order": {
        "id": "uuid",
        "partnerName": "客户名",
        "warehouseId": "uuid",
        "type": "outbound",
        "timestamp": 1724567890000,
        ...
      },
      "items": [
        {
          "id": "uuid",
          "orderId": "uuid",
          "itemName": "羊毛",
          "quantity": 360.0,
          "unitPrice": 6500.0,
          "itemNumber": 1,
          "pieceNumber": "A001",
          ...
        }
      ],
      "fees": [...]
    }
  ]
}
```

## 与MQTT同步的区别

| 特性 | MQTT同步（在线） | 离线同步 |
|-----|----------------|---------|
| **网络要求** | 需要互联网 | 无需网络 |
| **服务器** | 需要EMQX+Web后台 | 不需要服务器 |
| **实时性** | 实时同步 | 手动导入/导出 |
| **适用场景** | 多设备在线协同 | 离线备份、手动同步 |
| **成本** | 服务器运维成本 | 零成本 |

## 建议使用策略

### 混合使用（推荐）
- **有网络时**：使用MQTT在线同步（实时）
- **无网络时**：使用离线同步（备份）
- **定期备份**：每天导出一次作为保险

### 纯离线使用
- 如果不想维护服务器
- 完全使用离线同步
- 通过微信等即时通讯工具传输文件

## 测试清单

- [ ] 导出数据成功，文件生成
- [ ] 分享功能正常，能通过微信发送
- [ ] 导入数据成功，统计正确
- [ ] Merge策略：时间戳新的数据被保留
- [ ] Skip策略：本地数据未被覆盖
- [ ] Replace策略：导入数据覆盖本地
- [ ] 本地备份列表正确显示
- [ ] 从本地备份导入成功

## 后续优化方向

### 短期优化
1. 添加二维码分享：生成包含文件路径的二维码
2. 压缩导出文件：使用gzip压缩JSON
3. 增量导出：只导出未同步的数据

### 长期扩展
1. **局域网同步**：同一WiFi下自动发现设备
2. **蓝牙同步**：近距离直接传输
3. **NFC同步**：手机碰一碰即可同步
4. **云存储集成**：支持备份到百度网盘/阿里云盘

## 常见问题

### Q: 导出文件保存在哪里？
A: 保存在应用私有目录，可通过"本地备份文件"列表查看

### Q: 能导出到指定目录吗？
A: 目前通过分享功能发送，接收方可保存到任意位置

### Q: 导入会删除本地数据吗？
A: 不会。使用Merge策略会智能合并，使用Skip策略会跳过重复数据

### Q: 两台设备同时修改同一订单怎么办？
A: 使用Merge策略时，时间戳更新的数据会被保留

### Q: 能同步照片吗？
A: 当前版本只同步数据，照片路径会保留但文件需要单独传输

## 技术支持

如有问题，请检查：
1. Flutter版本是否支持（>=3.0）
2. 依赖是否正确安装（flutter pub get）
3. 权限是否正确配置（Android/iOS）
4. 文件路径是否可访问
