# CCTT 修复问题清单

生成日期：2026-06-14

## 执行规则

- 只有涉及界面 UI 修改、业务功能修改、同步语义修改、Web 管理能力范围、架构/安全等重大决策的问题，需要先向你确认。
- 测试基线、静态分析、明显注释/文档错漏、不会改变用户功能语义的代码卫生问题，由 Codex 直接处理。
- 每次只修复一个问题，修完后标记为“修复后待验收”，并请你确认。
- 你确认通过后，再把该项标记为“已确认”，然后进入下一项。
- 修复过程中不回退你已有的未提交改动。

## 状态说明

- 待确认：需要你决定方向。
- 待修复：方向已确认，可以开始修。
- 修复后待验收：代码已改，等待你确认。
- 已确认：你确认该项完成。
- Codex 自行处理：不涉及 UI/功能/重大决策，Codex 可直接修复。

## 已明确的产品/架构决策

1. v2.0 主单据 `Order` 是唯一正式数据流程；旧 `StockMovement` 数据删除，不再作为正式数据使用。
2. Web 后台只做云端数据库展示，只读查看，不做 Order 新建、编辑、作废、恢复、永久删除等管理操作。
3. 作废都需要作废原因，且允许自定义写入。
4. OCR 暂时不用，统一按照片留档处理。
5. 本轮不处理 MQTT 凭据和 TLS 证书安全性。
6. 本轮修自动同步：当前实现为前台 5 分钟定时同步 + 新增/编辑明细/结清/作废后触发静默同步，不引入应用退出后的后台任务依赖。
7. 后续不涉及界面 UI 修改、功能修改、重大决策的问题，由 Codex 自行确认和推进。

## 原待确认问题记录

1. v2.0 主单据 `Order` 是否作为唯一主流程？
   - 选项 A：保留旧 `StockMovement` 兼容和旧录入页，但首页/统计以 `Order` 为主。
   - 选项 B：彻底隐藏旧录入页，仅保留旧数据迁移和展示兼容。
   - 选项 C：两套都作为正式功能并列维护。
   - 当前状态：已确认，选择 B：彻底隐藏旧录入页，仅保留必要模型/表兼容，旧数据删除。

2. Web 后台的 `Order` 是否需要完整 CRUD？
   - 现状：Web 可查看单据列表/详情，但缺少新建、编辑、作废、恢复、永久删除等完整操作。
   - 请确认：Web 端是否只做查看，还是要和旧出入库流水一样完整管理。
   - 当前状态：已确认，Web 仅只读查看云端数据库。

3. 作废记录/单据是否需要统一“作废原因”？
   - 旧流水已有预设原因和自定义原因。
   - 新 `Order` 目前作废时没有原因输入。
   - 当前状态：已确认，需要统一作废原因并支持自定义写入。

4. 旧 `README.md` / `AGENTS.md` 中的 OCR 描述是否要改成“照片留档”？
   - 代码已移除 ML Kit OCR，实际功能是拍照/相册留档。
   - 请确认是否同步更新文档，避免以后继续按 OCR 方向开发。
   - 当前状态：已确认，OCR 暂时不用，统一照片留档。

5. MQTT 凭据和 TLS 证书策略是否现在处理？
   - 现状：Flutter 默认值和 `admin/.env.example` 中有明文 MQTT 账号密码，TLS 允许坏证书。
   - 请确认：本轮是否只修功能完整性，还是同时处理凭据/证书安全。
   - 当前状态：已确认，本轮不处理安全性。

6. 后台自动同步是否进入本轮？
   - 开发记录仍把 `connectivity_plus` / `workmanager` 自动同步列为待改进。
   - 请确认：本轮是否只修手动/启动同步，后台自动同步先不做。
   - 当前状态：已确认，本轮修自动同步；实现范围为前台定时和操作后触发。

## 待修复清单

### P0-01：Flutter 测试无法运行

- 证据：`flutter test` 失败，`test/widget_test.dart` 引用不存在的 `MyApp`。
- 影响：当前没有可用测试基线，后续修复无法用测试保护。
- 涉及文件：`test/widget_test.dart`、可能涉及 `lib/main.dart`
- 当前状态：已确认
- 修复记录：已将旧 Flutter 计数器模板测试替换为当前应用入口 `CcttApp` 的 smoke test。
- 验收方式：`D:\CCTT\flutter\bin\flutter.bat test` 至少能启动并通过基础 smoke test。
- 验证结果：2026-06-14 运行 `D:\CCTT\flutter\bin\flutter.bat test`，结果 `All tests passed!`。

### P0-02：v9 首次建库没有创建三张 Order 新表

- 证据：`DatabaseHelper._onCreate()` 只创建 `warehouses` 和 `stock_movements`，没有创建 `orders`、`order_items`、`order_fees`。
- 影响：新装或清库后依赖部分方法懒创建，插入明细/费用路径存在崩溃风险。
- 涉及文件：`lib/data/database_helper.dart`
- 当前状态：已确认
- 修复记录：数据库版本升级到 v10；首次建库直接创建 `orders`、`order_items`、`order_fees`；升级时清空旧 `stock_movements` 行，v2.0 起仅使用 Order 新数据。
- 验收方式：首次打开数据库后五张表都存在；新增 Order + Item + Fee 不报 no such table。
- 验证结果：新增 `test/database_helper_test.dart`，覆盖新库建表和 failed Order 重试；`flutter test` 通过。

### P1-01：同步成功判断文案不匹配

- 证据：`SyncService.syncPendingRecords()` 成功返回“同步完成...”，主页用 `contains('成功')` 判断是否成功。
- 影响：成功同步可能弹出“同步失败”对话框。
- 涉及文件：`lib/services/sync_service.dart`、`lib/pages/home_page.dart`
- 当前状态：已确认
- 修复记录：主页同步成功判断改为 `!pushResult.startsWith('同步失败') && pullResult.success`，不再依赖“成功”字样。
- 验收方式：成功同步时主页显示成功提示，不进入失败弹窗。

### P1-02：失败的 Order 不会被重试

- 证据：`getPendingOrders()` 只查询 `pending/syncing`，没有包含 `failed`。
- 影响：主单据同步失败后，可能无法通过再次点击同步重试。
- 涉及文件：`lib/data/database_helper.dart`、`lib/services/sync_service.dart`
- 当前状态：已确认
- 修复记录：`getPendingOrders()` 已纳入 `failed` 状态；同步失败后 Order 会重新进入待同步队列。
- 验收方式：手动把 Order 标记为 failed 后，再次同步会被纳入上传队列。
- 验证结果：新增数据库测试覆盖 failed Order 重试；`flutter test` 通过。

### P1-03：Order 下拉快照缺少云端删除清理逻辑

- 证据：`pullSnapshot()` 只清理快照中不存在的旧 `stock_movements`，没有清理 `orders`。
- 影响：Web 端永久删除或清理后的 Order 可能在手机端残留。
- 涉及文件：`lib/services/sync_service.dart`、可能涉及 `lib/data/database_helper.dart`
- 当前状态：已确认
- 修复记录：`pullSnapshot()` 只处理 Order 快照；本地已同步但云端快照不存在的 Order 会被删除，明细和费用同步清理；旧 `records` 会被本地清空。
- 验收方式：Master 快照中移除某个已同步 Order 后，手机下拉会删除本地对应 Order/Item/Fee。

### P1-04：Order 作废后没有自动同步，也没有作废原因

- 证据：`OrderDetailPage._voidOrder()` 直接 `voidReason: null`，且只 `_load()`，没有触发上传。
- 影响：作废状态不能及时同步到 Web/其他手机；无法追踪作废原因。
- 涉及文件：`lib/pages/order_detail_page.dart`、可能涉及 `lib/services/sync_service.dart`
- 当前状态：已确认
- 修复记录：Order 作废弹窗要求填写自定义作废原因；作废和结清后都会触发静默同步。
- 验收方式：作废 Order 可填写原因，作废后自动推送，其他端下拉后可看到作废状态和原因。

### P1-05：首页未推送数量、仓库数量、云端新增数量没有覆盖 Order

- 证据：`_unpushedCount`、仓库筛选数量、已作废入口主要统计 `_movements`，不是 `_orders`。
- 影响：默认新建 Order 后，首页角标和仓库数量不准。
- 涉及文件：`lib/pages/home_page.dart`、`lib/services/sync_service.dart`
- 当前状态：已确认
- 修复记录：首页列表、仓库数量、待推送角标、云端新增提示、已作废入口全部切到 Order；旧流水不再展示。
- 验收方式：新增/作废/失败的 Order 会正确体现在首页待推送角标、仓库数量、云端拉取提示中。

### P1-06：Web 后台 Order 仍是只读能力

- 证据：`admin/server/routes/orders.js` 只有 `GET /` 和 `GET /:id`；前端也只有列表和详情。
- 影响：如果 Web 要作为 Master 管理端，Order 无法在 Web 侧编辑、作废、恢复或永久删除。
- 涉及文件：`admin/server/routes/orders.js`、`admin/public/js/pages/orders.js`
- 当前状态：已确认
- 修复记录：按决策保留 Web Order 只读展示；未新增 CRUD。Web 初始化会清空旧流水表，MQTT 层忽略旧 `records`，快照只发布 Order 数据。
- 验收方式：按你确认的范围，Web 端具备对应 Order 操作，并在操作后发布 Master 快照。

### P2-01：OCR/照片留档文档和 UI 文案残留不一致

- 证据：`README.md`、`AGENTS.md` 仍写 OCR；旧录入页仍有“正在识别发票...”文案。
- 影响：开发记录、用户认知和实际功能不一致。
- 涉及文件：`README.md`、`AGENTS.md`、`pubspec.yaml` 注释、`lib/pages/add_record_page.dart`
- 当前状态：已确认
- 修复记录：`README.md`、`AGENTS.md`、`pubspec.yaml` 注释和旧录入页残留文案已统一为照片留档。
- 验收方式：文档和 UI 统一描述为“照片留档”，不再承诺本地 OCR。

### P2-02：旧设置页和长按配置已失去入口或语义过期

- 证据：`SettingsPage` 仍存在 MQTT/长按设置，但主页入口已被垃圾桶替代；长按编辑也已删除。
- 影响：代码残留增加维护成本，也容易误导后续开发。
- 涉及文件：`lib/pages/settings_page.dart`、`lib/services/settings_service.dart`、`lib/pages/home_page.dart`
- 当前状态：已确认
- 修复记录：删除无入口的 `settings_page.dart`；`SettingsService` 清理旧 HTTP 地址和长按配置，仅保留 MQTT 配置。
- 验收方式：按你选择保留、恢复入口或删除残留，代码和 UI 语义一致。

### P2-03：静态分析有 warning/info

- 证据：`flutter analyze` 报 9 个问题，包括未使用字段/导入、跨 async gap 使用 BuildContext、无意义 null 判断等。
- 影响：暂非主功能崩溃，但会掩盖后续真正问题。
- 涉及文件：`lib/pages/add_order_item_page.dart`、`lib/pages/add_order_page.dart`、`lib/pages/home_page.dart`、`lib/pages/order_detail_page.dart`
- 当前状态：已确认
- 修复记录：清理未使用字段/导入、无意义 null 判断、async gap、单行 if、废弃 `value` 参数等 analyzer 问题。
- 验收方式：`D:\CCTT\flutter\bin\flutter.bat analyze` 无 warning；若 info 暂不清理，需要你确认可接受。
- 验证结果：2026-06-14 运行 `D:\CCTT\flutter\bin\flutter.bat analyze`，结果 `No issues found!`。

## 已验证现状

- `D:\CCTT\flutter\bin\flutter.bat analyze`：通过，`No issues found!`。
- `D:\CCTT\flutter\bin\flutter.bat test`：通过，3 个测试全部通过。
- `node --check admin/server/**/*.js`：通过。
- `node --check admin/public/js/**/*.js`：通过。
